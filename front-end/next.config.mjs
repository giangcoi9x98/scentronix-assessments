/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['fashion-staging.s3.ap-southeast-1.amazonaws.com',"https://loremflickr.com"],
  },
};

export default nextConfig;
